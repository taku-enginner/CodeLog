<div class="flex items-center justify-center min-h-screen px-6">
  <!-- スライダー左 -->
  <div class="hidden lg:block w-1/3">
    <div class="swiper-container">
      <div class="swiper-wrapper">
        <div class="swiper-slide">
          <img src="/assets/editor_sample.png" class="rounded-xl shadow-md" />
        </div>
        <div class="swiper-slide">
          <img src="/assets/comments_highlight.png" class="rounded-xl shadow-md" />
        </div>
      </div>
    </div>
  </div>
  <div class="max-w-xl mx-auto bg-white shadow-xl rounded-3xl p-10 my-20 space-y-8 text-center">
    <h2 class="text-3xl font-extrabold text-gray-800">CodeLogへようこそ ✨</h2>

    <p class="text-base text-gray-600 leading-relaxed">
      GitHubのリポジトリを登録して、コードへの思考・気づきを記録しよう。<br>
      あなたの学びや工夫を“見える化”して、成長の証に。
    </p>

    <% if user_signed_in? %>
      <p class="text-lg text-gray-700">
        こんにちは、<span class="font-semibold text-purple-700"><%= current_user.email %></span> さん！
      </p>

      <div class="flex flex-col gap-4">
        <%= link_to "＋ リポジトリを登録する", new_repository_path,
          class: "w-full px-6 py-3 bg-purple-500 hover:bg-purple-600 text-white rounded-xl shadow transition text-base font-semibold" %>

        <%= button_to "ログアウト", destroy_user_session_path, method: :delete,
          class: "w-full px-6 py-3 bg-red-500 hover:bg-red-600 text-white rounded-xl shadow transition text-base font-semibold" %>
      </div>

    <% else %>
      <div class="flex flex-col md:flex-row justify-center items-center gap-4">
        <%= link_to "ログイン", new_user_session_path,
          class: "w-full md:w-auto px-6 py-3 bg-blue-500 hover:bg-blue-600 text-white rounded-xl shadow transition text-base font-semibold" %>

        <%= link_to "新規登録", new_user_registration_path,
          class: "w-full md:w-auto px-6 py-3 bg-green-500 hover:bg-green-600 text-white rounded-xl shadow transition text-base font-semibold" %>
      </div>
    <% end %>
  </div>

<!-- スライダー右 -->
  <div class="hidden lg:block w-1/3">
    <div class="swiper-container">
      <div class="swiper-wrapper">
        <div class="swiper-slide">
          <img src="/assets/chart_sample.png" class="rounded-xl shadow-md" />
        </div>
        <div class="swiper-slide">
          <img src="/assets/tag_chart.png" class="rounded-xl shadow-md" />
        </div>
      </div>
    </div>
  </div>
</div>