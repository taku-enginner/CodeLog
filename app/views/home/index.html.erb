<div class="max-w-md mx-auto bg-white shadow-lg rounded-2xl p-8 my-10 space-y-6 text-center">
  <h2 class="text-2xl font-bold text-gray-800">CodeLogへようこそ ✨</h2>
  <p class="text-sm text-gray-600">
    GitHubのリポジトリを登録して、コードへの思考・気づきを記録しよう。
    あなたの学びや工夫を“見える化”して、成長の証に。
  </p>

  <% if user_signed_in? %>
    <p class="text-lg text-gray-700">
      こんにちは、<span class="font-semibold text-purple-700"><%= current_user.email %></span> さん！
    </p>

    <div class="flex flex-col space-y-3">
      <%= link_to "＋ リポジトリを登録する", new_repository_path,
        class: "w-full px-6 py-3 bg-purple-500 hover:bg-purple-600 text-white rounded-lg shadow-md transition cursor-pointer text-base font-semibold" %>

      <%= button_to "ログアウト", destroy_user_session_path, method: :delete,
        class: "w-full px-6 py-3 bg-red-500 hover:bg-red-600 text-white rounded-lg shadow-md transition cursor-pointer text-base font-semibold" %>
    </div>

  <% else %>
    <div class="flex flex-col md:flex-row justify-center items-center space-y-3 md:space-y-0 md:space-x-4">
      <%= link_to "ログイン", new_user_session_path,
        class: "w-full md:w-auto px-6 py-3 bg-blue-500 hover:bg-blue-600 text-white rounded-lg shadow-md transition cursor-pointer text-base font-semibold" %>

      <%= link_to "新規登録", new_user_registration_path,
        class: "w-full md:w-auto px-6 py-3 bg-green-500 hover:bg-green-600 text-white rounded-lg shadow-md transition cursor-pointer text-base font-semibold" %>
    </div>
  <% end %>
</div>
