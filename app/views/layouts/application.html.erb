<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Myapp" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">  
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload", defer: true %>
    <%= javascript_include_tag "application", type: "module", defer: true %>

  </head>

  <body class="h-full flex flex-col min-h-screen bg-gray-100 text-gray-800">
    <!-- ヘッダー -->
    <header class="bg-white shadow-md px-6 py-4 flex justify-between items-center">
      <%= link_to "MyApp", root_path, class: "text-xl font-bold text-purple-600 hover:underline" %>

      <nav class="space-x-4">
        <% if user_signed_in? %>
          <%= link_to "リポジトリ一覧", repositories_path, class: "text-gray-700 hover:text-purple-600" %>
          <%= link_to "＋ 登録", new_repository_path, class: "text-gray-700 hover:text-purple-600" %>
          <%= link_to "ログアウト", destroy_user_session_path, method: :delete, class: "text-red-500 hover:text-red-600" %>
        <% else %>
          <%= link_to "ログイン", new_user_session_path, class: "text-blue-500 hover:underline" %>
          <%= link_to "新規登録", new_user_registration_path, class: "text-green-500 hover:underline" %>
        <% end %>
      </nav>
    </header>

    <!-- Flashメッセージ -->
    <% flash.each do |type, message| %>
      <div class="mx-auto max-w-3xl w-full px-4 mt-4">
        <div class="p-4 rounded text-white <%= type == "notice" ? 'bg-green-500' : 'bg-red-500' %>">
          <%= message %>
        </div>
      </div>
    <% end %>

    <!-- メインコンテンツ -->
    <main class="flex-1 container mx-auto px-6 pt-8 pb-24">
      <%= yield %>
    </main>

    <!-- 固定ボトムのフッター -->
    <footer class="fixed bottom-0 left-0 w-full bg-white border-t shadow z-50">
      <nav class="flex justify-around py-2 text-xs text-gray-600">
        
        <!-- 記録 -->
        <%= link_to "#", class: "flex flex-col items-center hover:text-blue-600" do %>
          <span class="text-xl">＋</span>
          <span>記録</span>
        <% end %>

        <!-- グラフ -->
        <%= link_to "#", class: "flex flex-col items-center hover:text-blue-600" do %>
          <span class="text-xl font-bold">n∩</span> <!-- 遊び心あるアイコン風 -->
          <span>グラフ</span>
        <% end %>

        <!-- 設定 -->
        <%= link_to "#", class: "flex flex-col items-center hover:text-blue-600" do %>
          <span class="text-xl">⟳</span> <!-- リロード風 or 歯車アイコンでもOK -->
          <span>設定</span>
        <% end %>

      </nav>
    </footer>


  </body>
</html>
